name: 🤖 Auto Generate QA Posts

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

# THÊM KHỐI NÀY VÀO ĐỂ TẮT CẢNH BÁO NODE 20
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Bạn có thể giữ nguyên '18' hoặc '20', dòng env ở trên sẽ tự xử lý ngầm

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔑 Create .env.local from GitHub Secrets
        run: |
          echo "${{ secrets.ENV_LOCAL }}" > .env.local

      - name: 🚀 Run generation script
        run: npm run gen:single

      - name: 💾 Commit and Push changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add .
          git commit -m "🤖 Auto-generated new QA post & updated pointer" || echo "No changes to commit"
          git push